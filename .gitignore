node_modules
.next
out

# Secrets / env
.env
.env*.local

# OS
.DS_Store

# User uploads — keep the folder, not its contents
public/uploads/*
!public/uploads/.gitkeep

# Sensitive content not safe to publish
data/admin.json
data/enquiries.json
